// gleam_bookshelf.gleam — Entry point. Connects to Postgres, starts the server.
// Equivalent of main.go and index.ts in the other implementations.

import gleam/io

pub fn main() {
  io.println("gleam_bookshelf — scaffold ready")
}
